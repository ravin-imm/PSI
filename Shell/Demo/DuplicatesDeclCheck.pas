program DuplicatesCheck;
const
   pi = 3.14;
   msg = "hello";
   msg = "world";
var
   i, j, k: integer;
   f: real;
   pi: real;
   k: string;

procedure PrintOn (ms: string);
begin
   writeln (ms);
end;

procedure pi (g: integer);
var
   l: integer;
begin
   l := g * g;
end;

begin 
   i := (3 + 4) * 2;
   f := pi * sin (3.5) + length (msg);
   writeln ("i=", i, ", f=", f);
end.